import React from 'react';

const Cards = (props) => {
    const { country } =props;
    console.log(country);
    return (
        <div>
            card;
        </div>
    );
};

export default Cards;